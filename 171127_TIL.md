# 11월 23일(목)

## 강의노트

### Computational Thinking 1회차

#### 0. 강사님 소개
박성백 강사님 (한번 수업을 들은 수강생에게는 평생 A/S를 제공!)
 - email: sungback@naver.com
 - blog: http://blog.naver.com/sungback

#### 1. 개요

* 앞으로 배워야 한다고 생각하는 언어
  * JavaScript: 웹 개발에서 알아야 하는 언어. 
  * Python: 머신러닝 쪽
  * Scala: 빅데이터 쪽

* 프론트엔드 3대장
  * React: 라이브러리
  * Angular: 프레임워크
  * Vue.js: 라이브러리

* 프레임워크와 라이브러리의 차이
  * 라이브러리: 공구상자 같은 느낌. 쓰는 데 부담이 없다. 자유도가 높다.
  * 프레임워크: 포크레인 같은 느낌. 포크레인을 조정하는 법이 따로 있다. 
    * 장점: 다 있음. 
    * 단점: 자유도가 떨어짐.
    
    일반적으로는 라이브러리를 쓰는 것을 추천. 자바스크립트 상에서까지 프레임워크를 쓸 필요가 있는지 고민. 프론트엔드는 주로 MVC 세 가지 중, View 쪽에 해당되는 기술에 중점을 두는 것이 좋다. 

* **내가 HTML, CSS를 배우는 이유는 `실제로 무언가를 만들기 위해서`이다.**
  * 리액트를 위한 리액트를 공부해서는 안 된다. 내가 만들고 싶은 사이트를 벤치마킹하여 그렇게 가고 싶으면 되는 것. 사소한 것이지만 공부하는 습관에서 달라지는 부분.
  * 목표를 잡아라. 여기에 필요한 기술을 때려넣자!


#### 2. Computational Thinking 간단 메모

* 서버 OS 점유율. Linux 96.91% (PC와 서버에는 갭이 있음!)
  * 윈도우에서도 Linux의 Bash가 뜬다. 윈도우가 Linux를 받아들였다.
  * 오픈소스가 대부분 Linux 기반.
  * 전 세계 수퍼 컴퓨터들은 대부분 Linux. 라이선스 비용을 감당하기에는 어렵기 때문!

#### 3. 웹 프로그래밍

##### 클라이언트와 서버
* Client가 Server에 보내는 것은 요청 (Request)
* Server가 Client에게 보내는 것은 응답 (Response)
  * 응답할 때 예전에는 HTML로 보내주었다. 하지만 웹 사이트 전체 중 일부만 변할 때도 HTML를 보내면 번거롭다.
    * 이를 Ajax로 변경하여 호출하고 JSON으로 보내주었다.

##### JSON (JavaScript Object Notation)
```
JSON:
{
  키: 값;
  키: 값;
}
```

* 자바스크립트에서 객체를 표현하는 방법.
* Object가 중요.
* 예전에는 Plain text로 주고받다가 이제는 효율이 떨어져서 XML이 나왔다.
  * XML 태그를 보면 너무 중첩되어 적혀있다. 네트워크를 타고 흐르기에 무거움.
  * 이제는 JSON으로 넘어왔다.
* Open API에 요청하면, 결과가 JSON으로 들어온다.
* 데이터를 주고 받는 표준.
* 다른 언어는 자신에게 맞게 JSON을 변환한다.
* MongoDB는 JSON으로 저장. DB까지 JavaScript 기술을 쓰게 되면 모든 것을 할 수 있는 시대가 되었다.

#### 4. 객체지향 프로그래밍과 함수형 프로그래밍

* 멀티스레드에서 100% 안전한 것은 없다.
 * thread A, thread B, thread C 가 들어간다면 어떤 결과가 나올지 아무도 알 수가 없다. => 동시에 들어가기 때문!
* 이를 해결하는 방법은 함수형 프로그래밍에서 찾고 있음. 그래서 OOP에서 함수로 넘어가고 있다.

왜 객체지향 프로그래밍이 인기를 얻었을까?
* GUI 시대가 되면서 OOP가 부품으로 만들어서 조립하면 된다는 의미에서 자리를 잡았다. 한편, FP는 학계로 이동했다.

* 하드웨어의 성능이 기하급수적으로 늘어나면서 (ex. 무어의 법칙) OOP보다 FP에 힘이 실리고 있다.
  * C10K Problem.
    * Client 10,000: 하나의 컴퓨터에서 클라이언트 만 개를 콘트롤할 수가 없다!
* 스레드를 많이 만든다고 해서 문제가 해결되지 않고, **함수형 프로그래밍** 을 이용해서 이벤트 방식을 사용한다. 

하드웨어가 이렇게 좋아졌는데도 안 된다.
서버 한 대당 100기가를 띄울 수는 없음.
한 사람당 하나의 스레드 10mb * 1,000명 = 10,000
스레드를 많이 만드는 것이 답은 아니다.

컨텍스트 스위칭 비용이 스레드 고치는 게 더 어렵다.
급격히 느려져서 스레드를 만드는 게 해결이 아님.

이걸 함수형 기법으로 풀었다! 이벤트 방식.

요청이 들어오면 하나씩 해결.

동기와 비동기. 비동기가 중심이 되는 세상.

자바스크립트 자체가 싱글스레드.
편법을 사용하여 비동기로. 프로그램 실행 시간보다는 응답 시간이 더 중요하다!

ex. A식당에 갔더니 아무런 대답이 없다가 10분 뒤에 물을 가져다 줌 / B식당은 일단 잠깐 기다리라고 얘기해주고 10분 뒤에 물을 줬다.

Non-blocking. 동기방식이 아닌 비동기 방식.
동기방식에서 비동기방식으로 무게중심이 이동. 응답속도. 프로그램 효율성 등에 따라 달라짐.
진동벨.

자바스크립트에서 비동기 처리를 하려면 항상 오른쪽으로 던져야 함. 

수많은 프로세스들이 있음!

프로그램 |  프로세스 | 스레드

프로그램: 엑셀을 클릭해서 실행시키기 전 상태.
프로세스: 실행한 상태. 메모리에 올라와있는 상태.
스레드: 프로그램 안에 하위 프로그램 / 하나의 프로세스에 종속된 작은 작업단위

스레드를 많이 쓰면 쓸수록 느려진다.
그래서 최근에는 프로세스가 무거워서 프로세스 안에 스레드를 띄우는 형태로 발전해왔다가,
스레드를 쓰지 말고 프로세스를 쓰자는 형태.
실제 운영체제 프로세스가 아니라, 프로그램적으로 작은 프로세스를 여러 개 띄워서 쓰자.

scala. akka. 프로세스 하나가 10kb밖에 되지 않는다.

프로세서는 CPU Core간에 협업이 가능.
스레드는 프로세스 안에 갇히므로 사용할 수가 없음.

함수형 언어가 어떻게 멀티스레드를 해결했나?

- 근본을 바꾼다.
- 데이터를 수정. 불변 데이터. 한번 만들어지면 변하지 않게 만든다.
- 복사해서 준다. 사본을 준다. 원본 데이터를 불변으로 만든다. 읽기전용으로.
- 불변 이야기를 하는 이유는 자바스크립트에도 불변이라는 키워드가 생김. ES6에서!
- 함수형 언어의 장점을 쓰기 위해서.

불변 데이터를 마음대로 읽어봐라! 원본을 고치지 않는 방식으로.
그래서 함수형 언어로 바뀌게 되었음.

ES6 문법

arrow function

(reg, res) => {}
   입력      과 출력


http
200: ok
404: not found
500: error

'`': 백 쿼트.

Express 설치 사용

npm install express-generator

MVC 패턴. 서버 쪽의 패턴.

Model
View
Controller

프론트엔드에도 MVC 패턴이 있음!
프론트엔드의 MVC는 과연 무엇일까?

M: Data
V: 화면
C: 데이터 처리

프론트엔드에서 프론트엔드 기술 3가지

M: html
V: css
C: js

프론트엔드에서의 MVC. HTML 태그에서 `<h1>`제목`</h1>` 은 데이터를 품고 있는 태그라고 할 수 있음.

백엔드에서는 데이터가 99% Database에 들어있다. DB는 엑셀 시트라고 생각하면 편하다.
View는 변환되기 전 템플릿 (ex. .jsp, .ejs, .jade, .php, .asp...) => 나중에 HTML로 변환된다
Controller는 전체를 관리하는 것.

별도의 모듈이 있음.
html은 정적인 데이터라 변화를 주어야 함.
그래서 `<% title %>` 같은 ejs 코드가 필요하다. 이전부터 asp, jsp, php 등이 쓰던 규칙.
server-side lang을 보면 익숙해질 수 있음.

웹 프로젝트.
스캐폴딩. `express --ejs 프로젝트명`

   create : exp1
   create : exp1/package.json
   create : exp1/app.js
   create : exp1/public
   create : exp1/routes
   create : exp1/routes/index.js
   create : exp1/routes/users.js
   create : exp1/views
   create : exp1/views/index.ejs
   create : exp1/views/error.ejs
   create : exp1/bin
   create : exp1/bin/www
   create : exp1/public/javascripts
   create : exp1/public/images
   create : exp1/public/stylesheets
   create : exp1/public/stylesheets/style.css

   install dependencies:
     $ cd exp1 && npm install

   run the app:
     $ DEBUG=exp1:* npm start


package.json 파일.
수많은 package 가 돌아간다. 의존하는 것까지 같이 설치되기 때문.

기본 세팅이 다 되어 있어서 설치되어 있음.

`<form>`태그의 메소드: GET과 POST가 있음!
링크를 클릭해서 들어갈 때, 주소표시줄: GET
글쓰기, 전송: POST

결론: GET방식으로 왔을 때 받는 것.

GET방식 '/'로 요청했을 때.

CRUD    (SQL)
Create  Insert  
Read    Select
Update  Update
Delete  Delete

전세계에서 가장 많이 쓰는 웹서버. Apache. => nginx로 넘어가는 단계. node.js처럼 비동기 이벤트 방식.

동시 접속자수.
JS는 자체의 멀티스레드는 없고, 운영체제의 스레드를 빌려쓴다. 그래서 빠르다.

마우스 클릭하는 게 이벤트. 계속 대기하는 게 아니라 발생할 때만 처리해준다.
무한루프 돌며 기다리는 것이 아님. 신호가 갈 때마다 처리. 이벤트는 계속 지켜보는 것이 아님.
스레드는 계속 지켜보는 것.

운영체제 단에서 처리해준다. 이게 싱글스레드.
싱글스레드의 단점. 이게 죽으면 바로 멈춘다. 실제 웹서버도 동시에 여러 개 띄워놔서 살릴 수 있도록.
로드밸런싱.
단일서버에서는 하나만 띄울 수 없음.
자바, 파이썬, 장고 등. 실제로는 홀수로.

배포하고 / 띄우고 / 배포하고 띄우고 

CPU / 메모리 / 하드디스크.
CPU. 집 앞에 수퍼.

DB는 느리다. 디스크에서 읽기 때문. 디스크에서 읽지 않고 메모리에서 읽는 DB가 나옴.
메모리가 1TB.
(메모리에서 모든 것이 돌아가는 DB도 있음.)

빅데이터. 하둡.
http://engineering.vcnc.co.kr/2015/05/data-analysis-with-spark/

apache spark는 메모리에서 처리.
함수형 언어의 패턴. chaining을 통하여 전달.

빅데이터의 요구가 가장 먼저 있었던 곳이 구글.

앞으로 어디까지 공부를 해야 하는가.
IT. Information Technology. 2개의 단어로 추상화를 한다면?
 - Data 저장
 - Data 처리

어디다 어떻게 저장할 것인가
어떻게 처리할 것인가

spark 함수형 언어
DB, RDB.
MongoDB는 키: 값;

데이터를 어떻게 가공하고 관리하는 것이 가장 쉬울 것인가. 이게 자료구조의 한 축.
어떤 알고리즘으로 돌려야 가장 빨리 결과가 도출될 것인지.

자료구조가 데이터 저장.
알고리즘이 데이터 처리.

지금 현재 IT회사를 보면 신입/경력. 면접만 보고 끝났지만 이제는 테스트를 한다.

## 오늘 한 일

### 1. 
### 2. 



### 3. 오늘 읽은 링크
- []()
- []()

***

## 회고

### 1. 

### 2. 

### 3. 

### 4. 